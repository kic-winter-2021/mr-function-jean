<?xml version="1.0" encoding="UTF-8"?>
<!-- /mapper/mybatis-config.xml -->
<!DOCTYPE configuration PUBLIC "-//mybatis.org/DTD config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<properties resource="mapper/mybatis.properties" />
	<typeAliases>
		<typeAlias type="mr.function.User" alias="User" />
		<typeAlias type="mr.function.Item" alias="Item" />
		<typeAlias type="mr.function.Cart" alias="Cart" />
	</typeAliases>
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED"> <!-- Connection 객체 -->
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>
	<mappers><!-- sql 구문 -->
		<package name="mapper" />
	</mappers>
</configuration>